// Declare and initialize variables for different data types (string, number, boolean).

const str = "Abhishek"
console.log(str);

const num = 563
console.log(num);

const bool = true
console.log(bool);